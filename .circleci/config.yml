version: 2
jobs:
  one:
    docker:
      - image: unicef/etools:test-base-p3
    steps:
      - run:
          name: Check
          command: |
            env | sort
  two:
    docker:
      - image: unicef/etools:test-base-p3
    steps:
      - run:
          name: Check
          command: |
            env | sort
  three:
    docker:
      - image: unicef/etools:test-base-p3
    steps:
      - run:
          name: Check
          command: |
            env | sort


workflows:
  version: 2
  build_and_test:
    jobs:
      - one
      - two
      - tree
